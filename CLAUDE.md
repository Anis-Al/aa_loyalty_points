# aa_loyalty_points — working log

Implements `PRD_dt_loyalty_points.md` (sibling of this module, in `odoo/mnt/`).

## How to work on this module

Standing instructions from the client. **They outlive any single conversation —
follow them even with no other context.**

1. **Verify lot by lot.** Build one lot, run the suite, log it here, then start the
   next. Do not build several lots and test at the end.
2. **Log every change in this file.** Every code change gets an entry in the
   change log below, with the date and what it does. Assume the conversation that
   produced it is gone; this file is the only record.
3. **Design rationale lives in this file**, in "Design notes" at the bottom, not
   only in code comments. The manual acceptance pass lives in `TEST_MANUAL.md`
   next to this file — keep it in step when behaviour changes.
4. **`mconfort` is the working database.** Install and test there.
5. Wording in the product is **English**; French lives in `i18n/fr.po`, and in
   `body_html@fr_FR` for the email body. Keep both in step with the source.

| Lot | Scope | Status |
|---|---|---|
| D | Points on customer credit notes | **done** — 16 tests green |
| C | Points statement on the card report + email | **done** — 8 tests green |
| A | Portal: `/my/loyalty` counter, page, history | **done** — 12 tests green |
| B | Sale order: coupon stat button + points totals | **done** — 13 tests green |
| E | Coupon report redesigned (own layout, no header/footer) | **done** — 44 tests green |

Last full run: 2026-09-01 on `mconfort` — **44 tests, 0 failed, 0 errors**.

### Open — not done yet

- **`refund_policy` / `allow_negative_points`** ship with defaults and a form
  group but have never been exercised on live data, only in tests.
- PRD §C2 phase 2 (statement cron, `last_statement_date`) and the `email_from`
  change on the coupon template: still not built, still no decision.

Product wording is English; **French ships in `i18n/fr.po`**, except the coupon
email body, which cannot live in a `.po` — see the design note on
`body_html@fr_FR`.

> ## `next_order_coupons` is in scope — deviation from PRD §D8
>
> **Every one of the 1046 loyalty cards on `mconfort` belongs to program 2,
> `next_order_coupons` ("Bons de réduction pour la prochaine commande").** The
> eWallet program (id 1, "Gift Cards") has zero cards. No `loyalty` program exists.
>
> PRD §D8 puts `next_order_coupons` out of scope on the grounds that it has "pas de
> solde persistant". **The live data contradicts that.** Card 1256 holds 5301
> points from a single history row (`Commande S01689`, issued 5301), 397 cards
> carry a non-zero balance, and 17538 points have already been redeemed across 18
> order lines. These cards behave exactly like loyalty cards: earned on one order,
> spent on later ones, tracked in `loyalty.history`. Refund that order under the
> PRD as written and the customer keeps a 5301-point coupon for a purchase they
> were paid back for — the exact leak the PRD was written to close.
>
> **Client decision 2026-08-31: bring it into scope, and go further than a
> deduction — an unused coupon must disappear on a credit note, the way core
> already removes it when the order is cancelled.** So:
>
> - `next_order_coupons` is in `BALANCE_PROGRAM_TYPES`.
> - After the points move, a card is **archived** when it is non-nominative, was
>   generated by the refunded order, has never been used, and now sits at zero or
>   below. `button_draft` on the credit note un-archives it and restores the points.
>
> Archived rather than hard-unlinked on purpose: a credit note is reversible by
> design, and `loyalty.history.card_id` is `ondelete='cascade'`, so deleting the
> card would take the audit trail with it and make the reset-to-draft path
> impossible. Archiving reaches the same outcome — the coupon cannot be used and
> disappears from every default search — while staying reversible. Switch
> `card.action_archive()` to `card.unlink()` in
> `_apply_loyalty_refund_on_order` if a hard delete is ever wanted, but the revert
> path then has nothing to restore.
>
> **The portal follows the same constant.** `_get_portal_loyalty_card_domain()` uses
> `BALANCE_PROGRAM_TYPES` rather than Odoo's narrower `['loyalty', 'ewallet']`, and
> `cards_per_programs` is recomputed with it so the **native sidebar agrees with our
> page**. Before this, zero cards matched the portal domain and every customer saw
> an empty portal; now 1046 cards match and 28 of the 49 portal users see their
> balance. Program 2 has `portal_visible = True`, so showing these to customers is
> what the configuration already asked for.

---

## Change log

### 2026-08-31 — Lot D, points on credit notes

Created the module. Added `models/account_move.py` (recovery on `_post`, revert on
`button_draft` / `button_cancel`), `models/sale_order.py` (`_action_cancel` guard),
`models/loyalty_program.py` (`refund_policy`, `allow_negative_points`),
`models/loyalty_history.py` (portal URL for credit notes),
`views/loyalty_program_views.xml`, `__init__.py` with the `uninstall_hook`, and
`tests/test_refund_points.py`. 12 tests green.

### 2026-08-31 — Lot C, points statement

Added `models/loyalty_card.py` (`_shows_points_statement`,
`_get_statement_history`), `report/points_statement_templates.xml` (inherits
`loyalty.loyalty_report`), `data/mail_template_data.xml` (overrides `body_html` on
`loyalty.mail_template_loyalty_card`), and `tests/test_statement.py`. Moved
`BALANCE_PROGRAM_TYPES` out of `account_move.py` into `loyalty_program.py` so both
lots share it. 7 tests green, 19 total.

### 2026-08-31 — subject decision + guard test

Client decision: keep Odoo's subject, on the condition that the new body fully
replaces the old one. Verified in the database, then pinned with
`test_our_body_replaces_the_stock_one`. 20 tests total.

### 2026-08-31 — moved to `mconfort`

Installed on `mconfort` and ran the suite there: 20 tests, 0 failed, 0 errors.
Verified afterwards that the module is `installed`, the stock email body is gone,
the subject is unchanged, and all 3 programs defaulted to
`refund_policy = 'proportional'`.

### 2026-08-31 — comments migrated out of the source

Client decision: this file is the single source for design rationale. Every
explanatory comment, comment block and docstring was removed from the `.py` and
`.xml` files and now lives in "Design notes" below. What was kept in the source:
the one-line licence headers, the `# === SECTION === #` dividers, and `help=` /
`string=` on fields, which are user-facing UI text rather than commentary.

**Consequence to keep in mind:** reading a source file alone no longer tells you
why anything is the way it is. `models/sale_order.py` in particular is now four
lines with no hint that removing them corrupts card balances. Anyone editing this
module must read "Design notes" first. Suite re-run after the strip: 20 tests,
0 failed, 0 errors.

### 2026-08-31 — Lot A, portal

Added `controllers/portal.py` (`loyalty_count` counter, `/my/loyalty`,
`/my/loyalty/history`), `views/portal_templates.xml` (home tile, both pages,
breadcrumbs) and `tests/test_portal_loyalty.py`. 10 tests green, 30 total.
`--http-port` had to be added to the test command, see below.

### 2026-08-31 — Lot B, sale order

Added `available_coupon_count` + `action_view_available_coupons()` and the four
points-total fields to `models/sale_order.py`, `views/sale_order_views.xml`, and
`tests/test_sale_order_points.py`. 13 tests green, 43 total.

`test_the_totals_drop_back_to_zero_after_cancelling` failed on the first run
(`100.0 != 0`): a cancelled order leaves the `sale` state, so it fell into the
draft branch and re-estimated its points from lines that are still there. Fixed by
short-circuiting `state == 'cancel'` to zeros.

### 2026-08-31 — live configuration finding

Audited what is actually on `mconfort` and found that all 1046 cards are
`next_order_coupons`, which the PRD excludes. See the box at the top of this file.

### 2026-08-31 — next_order_coupons brought into scope, unused coupons archived

Client decision. Added `next_order_coupons` to `BALANCE_PROGRAM_TYPES`, and made
`_apply_loyalty_refund_on_order` archive a card that is non-nominative, was
generated by the refunded order, unused, and left at zero or below.
`_revert_loyalty_refund` un-archives it. Four tests added in
`TestRefundNextOrderCoupon`. 47 tests green.

Two traps hit while writing the fixture, both worth remembering:

- **`loyalty.program` presets do not apply on `create`.** Creating with only
  `program_type='next_order_coupons'` yields `rule_ids == []`, `applies_on ==
  'current'` and `trigger == False` — the program is not actually a next-order
  program at all, and generates no coupon. `applies_on`, `trigger`, `rule_ids` and
  `reward_ids` must all be passed explicitly, the way
  `sale_loyalty.tests.common` does for its gift-card program.
- **Piping a script into `odoo-bin shell` from PowerShell prepends a BOM** and
  dies with `SyntaxError: invalid non-printable character U+FEFF`. Setting
  `$OutputEncoding` does not fix it. Pipe from the Bash tool instead.

### 2026-08-31 — portal widened to the same constant

`/my/loyalty` was empty for every customer: zero cards matched the
`['loyalty', 'ewallet']` domain inherited from Odoo. Switched
`_get_portal_loyalty_card_domain()` to `BALANCE_PROGRAM_TYPES`, recomputed
`cards_per_programs` so the native sidebar matches. Now 1046 cards match and 28 of
49 portal users see a balance. Two tests added, 49 total.

The sidebar image 404s for program types core never expected there, so the first
attempt rewrote its src per type — and broke `/my` with a 500
(`KeyError: 'program'`), because the `//img` xpath matched an unrelated image in
`portal.side_content`. Client call: drop images entirely rather than patch them.
`loyalty_buttons_plain` now removes the `<img>` and the home tile passes no `icon`,
which is what the rest of `/my` already looks like.

### 2026-08-31 — history moved onto `/my/loyalty`, voucher icon added

Client call: show the movements on the page itself rather than behind a second
route. `/my/loyalty` now renders totals, a compact card list, and the paginated
history table (Document, Date, Program, Earned, Used). The
`/my/loyalty/history` route and its template are **gone**, and the breadcrumb no
longer has a second level. Pagination moved to `/my/loyalty/page/<int:page>`.

The client supplied `discount-voucher-icon.svg`, so the portal has an icon again:
shipped as `static/src/img/voucher.svg`, used by the home tile and by the sidebar
for every program type that has no icon of its own.

**Renaming a view's xmlid mid-flight breaks the update.** Going from
`loyalty_buttons_plain` (which *removed* the `<img>`) to `loyalty_buttons_icon`
(which *sets attributes* on it) failed with a `ParseError`: `-u` creates the new
record while the old one is still active, so the attribute xpath had no node to
attach to — Odoo only drops obsolete records at the end of the load. Fixed by
removing the template, running `-u` once to let Odoo delete the stale view, then
reintroducing it. **Pick a view xmlid and keep it.**

### 2026-08-31 — portal blocks made plain

Client call: drop the images rather than patch them. `loyalty_buttons_plain`
removes the `<img>` from the sidebar card outright and the home tile passes no
`icon`, which is what the rest of `/my` already looks like — `portal_docs_entry`
hides its icon behind `d-none` unless the optional "Use Pictograms" template is on.
Net effect is less code than the fallback it replaced, and no dependency on the
loyalty module's static assets. A test asserts `/loyalty/static/src/img/` appears
nowhere on `/my`.


### 2026-08-31 — points block dropped, coupons open in a dialog

Client call, two changes on the sale order form.

**1. The points block under the totals is gone.** `sale_loyalty` already prints
**Carte de fidélité / Émis(e) / Utilisé(e)** right below it, so ours was a second
reading of the same thing. Removed the `group name="sale_total"` block from
`views/sale_order_views.xml` and, with it, `points_earned`, `points_spent`,
`points_balance_after`, `points_label`, `_compute_points_totals` and
`_get_estimated_points` from `models/sale_order.py` — nothing else read them. The
7 tests in the "POINTS TOTALS" section of `tests/test_sale_order_points.py` went
with the fields; 42 tests remain.

Note what this deletes along with the block: the native one only appears **once
the order is confirmed and a card exists**. The quotation-stage estimate Lot B
built (`_get_applicable_program_points()`, see the design note that used to
justify it) no longer has a consumer. Bring the fields back if the estimate is
ever wanted again — the reasoning is in the git-less history of this file, above.

**2. The Coupons stat button opens a dialog.** `action_view_available_coupons()`
no longer reuses `loyalty.loyalty_card_action`; it returns its own act_window with
`target='new'` and `context={'dialog_size': 'large'}`, pointing at a new list view
`loyalty_card_view_list_dialog` in `views/sale_order_views.xml`. That view carries
`widget="CopyClipboardChar"` on `code`, so each row has a clipboard button
(`web`'s `CopyClipboardChar`, registered in
`web/static/src/views/fields/copy_clipboard/copy_clipboard_field.js:105`).

A separate list view rather than an inherit of `loyalty.loyalty_card_view_tree`:
inheriting would put the clipboard button on the **Coupons menu everywhere**, and
would drag in the core view's `js_class="loyalty_card_list_view"` and its **Send**
button, neither of which belongs in this dialog.

### 2026-08-31 — voucher.svg given an intrinsic size

The `/my` "My Points" tile did not sit like the other tiles: `voucher.svg` carried
only a `viewBox` and no `width`/`height`. Nothing in the portal styles sizes
`.o_portal_icon` — every core tile icon (`portal/static/src/img/portal-addresses.svg`,
`sale/.../bag.svg`, `account/.../bill.svg`) carries `width="64" height="64"` and
relies on that intrinsic size. Without it the browser falls back to 300×150.
Added `width="64" height="64"` to the `<svg>` root; the non-square viewBox
(112.68×122.88) letterboxes inside it, so the artwork is not distorted.

Static file — no `-u` and no service restart needed, but the old file is cached by
the browser: hard-refresh `/my` to see it.

### 2026-08-31 — "My Points" tile moved last on `/my`, after Connection & Security

Client call. `portal.portal_my_home` lays out five sibling divs inside
`o_portal_docs`: `portal_alert_category`, `portal_client_category`,
`portal_service_category`, `portal_vendor_category`, and last
`portal_common_category`, which holds **Addresses** and **Connection & Security**.
Every document tile in core (sale, account, purchase, payment, …) goes
`position="inside"` `portal_client_category`, and that is where ours was too.

Two changes:

- The tile now targets **`portal_common_category`**, so it renders after
  Connection & Security rather than among the document tiles.
- Inherit `priority` raised `30` → **90**, the highest of the eight
  `portal.portal_my_home` inherits on `mconfort` (appointment 16,
  account_payment / sale / mrp_subcontracting 20, purchase 25, account 30,
  payment 60). Order inside a category div is the order the inherits are applied,
  so this pins the tile last. A future module inheriting above 90 would slot in
  after us.

`portal_common_category` has no `t-if`, so the
`<t t-set="portal_client_category_enable" t-value="True"/>` xpath was dropped —
the div is always rendered.

The tile keeps `placeholder_count="loyalty_count"` and therefore keeps
`portal_docs_entry`'s `d-none` until the counter loads. Unlike the two config
cards beside it, it stays hidden for a customer with no card, which is the
intended behaviour. Both classes carry `order-2`, so DOM order decides.

Suite re-run after the move: **42 tests, 0 failed, 0 errors**.

### 2026-08-31 — renamed `dt_loyalty_points` → `aa_loyalty_points`

Client call: the module moves under Anis Alim's authority. Technical name, display
name, author and every external ID prefix changed in one pass.

| | Before | After |
|---|---|---|
| Directory / technical name | `dt_loyalty_points` | `aa_loyalty_points` |
| Manifest `name` | "Loyalty - Points on portal, sales orders and credit notes" | "Loyalty Points Extension" |
| `author` / `maintainer` | *absent* | "Anis Alim" |
| `version` | `19.0.1.0.0` | `19.0.1.1.0` |
| External IDs | `dt_loyalty_points.*` | `aa_loyalty_points.*` |
| Licence header | "See LICENSE file for licensing details." | "by Anis Alim. Licensed under LGPL-3." |

The eight own xmlids keep their local names (`portal_my_loyalty`,
`loyalty_card_view_list_dialog`, …) — only the module prefix moved, so the
references that had to change were the three that spell the prefix out:
`env.ref('aa_loyalty_points.loyalty_card_view_list_dialog')` in
`models/sale_order.py`, `request.render('aa_loyalty_points.portal_my_loyalty')` in
`controllers/portal.py`, and the `/aa_loyalty_points/static/src/img/voucher.svg`
asset URL in `views/portal_templates.xml` and its test. The config parameter key
became `aa_loyalty_points.statement_max_lines`.

The old header claimed a `LICENSE` file that has never existed in this module;
rather than add one, the header now states the licence inline and matches
`'license': 'LGPL-3'` in the manifest.

**The rename was a DB migration, not a reinstall.** Uninstalling is not an option
here: `uninstall_hook` purges every `loyalty.history` row with
`order_model = 'account.move'`, which is the module's whole audit trail. Two
statements per database instead:

```sql
UPDATE ir_module_module SET name='aa_loyalty_points' WHERE name='dt_loyalty_points';
UPDATE ir_model_data   SET module='aa_loyalty_points' WHERE module='dt_loyalty_points';
```

Run on **`mconfort`** (1 module row, 29 `ir_model_data` rows), **`dev`** (18) and
**`dt_loyalty_test`** (22). Checked first and found nothing else to migrate: no
`ir_config_parameter` row held the old key, no module depended on ours, and the
three `ir_ui_view` rows containing the old string are our own templates, rewritten
by the `-u` that followed. `demo`, `mconfort-old` and `mine` never had the module.
Reverse the two statements to roll back.

**Renaming the directory needs a shell that is not sitting inside it.** Windows
refuses `Rename-Item` on a folder that is any process's working directory —
`Le processus ne peut pas accéder au fichier, car il est utilisé par un autre
processus` — and this session's working directory *was* the module. Renaming a
subfolder succeeded, which is how the lock was pinned to the root. Worked around
by creating `aa_loyalty_points` and moving the children across, leaving an empty
`dt_loyalty_points` husk to delete afterwards. A directory with no `__manifest__.py`
is ignored by the addons loader, so the husk is inert while it survives.

Also deleted an 83 KB file literally named `-` in the module root — a stray
`--logfile=-` redirect from an earlier test run.

Left alone on purpose: **`PRD_dt_loyalty_points.md`** in `odoo/mnt/` keeps its
name. It is the client's specification document, not part of the module, and the
reference to it at the top of this file still points at the real filename.

Suite re-run on `mconfort` under the new name: **42 tests, 0 failed, 0 errors**.

### 2026-08-31 — French translation (`i18n/fr.po` + email body)

Client report: the coupon email and the card report stayed English for a French
user, and the exported translation template had no keys for the email.

**Both symptoms, one root cause each.**

**1. The report/portal keys were there all along — the module just shipped no
`i18n/`.** Verified by exporting the POT for `aa_loyalty_points`: 48 keys,
including every term of `loyalty_report_points_statement` and
`portal_my_loyalty`. Added `i18n/fr.po` covering them. Skipped on purpose: the
`t-out` placeholder samples (`0 Points`, `10 Points`, `100 Points`,
`Order S00001`) which never render, and the inherited `ir.model` /
`ir.model.fields` labels (`ID`, `Display Name`, `Journal Entry`, `Sales Order`,
`Loyalty Coupon`, …) which core already translates on the same records.

**2. The email keys can never be in our `.po`, and the French body was stale.**
`loyalty.mail_template_loyalty_card` has `ir_model_data.module = 'loyalty'`. The
export is driven by that column (`tools/translate.py:1399`), so `body_html` terms
belong to the **loyalty** module's POT, not ours — we only write a field on a
record we do not own. Worse: `body_html` is `translate=True` (model-level jsonb,
keys `en_US` + `fr_FR`), and our data file wrote **only `en_US`**. The `fr_FR`
slot still held Odoo's original French body. **Every French customer was getting
the stock Odoo email**, with no balance line and none of our wording, while
`test_our_body_replaces_the_stock_one` passed because it reads `en_US`.

Fixed by adding a `body_html@fr_FR` field to `data/mail_template_data.xml`. Odoo
19 reads `@lang`-suffixed fields straight out of a module's data files
(`ir_module.py:971` → `XMLDataFileReader`, `translate.py:798`); the record loader
skips any field whose name contains `@` (`convert.py:395`). Same convention as
`l10n_ar/data/l10n_latam_identification_type_data.xml`.

Three traps, all hit:

- **`type="html"` is illegal on a `@lang` field.** The RelaxNG schema
  (`import_xml.rng:108-118`) requires `type="html"` to contain at least one child
  *element*, and the reader only takes `field.text` — so the value must be plain
  text. Result: `AssertionError: Element odoo has extra content: record, line 4`,
  which names the record, not the field. Ship the markup as CDATA with **no
  `type`** attribute.
- **A plain `-u` does not overwrite an existing translation.**
  `overwrite_existing_translations` defaults to False, and `fr_FR` already had
  Odoo's body. The deploy needs `--i18n-overwrite` **once**:

  ```powershell
  ... -d mconfort -u aa_loyalty_points --i18n-overwrite --stop-after-init --no-http
  ```

- **Translating the module broke three tests that asserted English labels.**
  `mconfort`'s website is French-only (`lang_url_code: "fr"`), so `/my/loyalty`
  renders `fr_FR` regardless of the portal user's `lang` — setting `lang: 'en_US'`
  on the test users changes nothing and was reverted. `test_the_history_is_on_the_page_itself`
  now asserts language-neutral anchors (`o_portal_my_doc_table`, five `<th>`)
  instead of "Earned"/"Used". The two `test_statement.py` failures were different:
  `loyalty.loyalty_report_i18n` renders in the **card partner's** language, so
  `partner_a` is now created with `lang: 'en_US'`, which is what those tests always
  meant.

Suite re-run: **42 tests, 0 failed, 0 errors.**

### 2026-08-31 — plain email body, and points shown in money

Client call, two changes on `loyalty.mail_template_loyalty_card`.

**1. No background styles.** The body was a `<table>` with
`background:#ffffff; color:#333333` and a grey chip
(`background-color:#F1F1F1`) around the code. Both gone; the body is now plain
`<p>` blocks in a bare `<div>`, no `style` attribute anywhere. Same in both
languages. Asserted on the live card: `'background' in body` is False.

**2. The balance carries its money value.** "Votre solde actuel : 430 Point(s) de
réduction (4,30 €)". New `loyalty.card._get_points_money_value()` in
`models/loyalty_card.py` returns the formatted amount, or `''` when the program
has no per-point money rate.

The rate comes from the program's own reward: the first
`reward_type == 'discount'` with `discount_mode == 'per_point'`, whose `discount`
is the currency amount **per point**. On `mconfort` that is reward 5 on program 2
at `0.01 €`/point, which is why 430 points read as 4,30 €. Formatted with
`formatLang(..., currency_obj=program.currency_id)`, so it follows the reader's
language — comma in French, dot in English — and the program's currency.

`discount in (0, 1)` returns `''`. Rate 1 means the money figure equals the point
figure, which is the eWallet case (program 1, `portal_point_name = "€"`): printing
"100 € (100,00 €)" is noise. A program with no per-point reward — a percentage
discount, a free product — has no rate at all and also returns `''`, so the
parenthesis simply does not render.

**Not implemented: `discount_max_amount`.** A program capping the discount per
order would show a money value higher than what a single order can actually
redeem. No live program sets it (all three are `0.0`). Clamp in
`_get_points_money_value` if one ever does.

One test added, `test_the_balance_shows_its_money_value`, covering both branches.
It compares against the helper's own output rather than a hardcoded `"1.20"` —
the first version failed because the test env formats as `$ 1,20`, currency
symbol and decimal comma both coming from the environment, not from us.

**This change touches `models/`, so the live service must be restarted** — see
"Deploying a change to `mconfort`". Until it is, the running process has the old
`loyalty.card` class and rendering the template raises on the missing method.

**3. Signature dropped.** The `<t t-if="object._get_signature()">` block and its
`--` separator are gone from both bodies. It duplicated the sign-off: the body
already ends "L'équipe M'Confort", and the signature rendered the same string
again two lines below. `_get_signature()` itself is untouched — only this template
stopped calling it.

Suite: **43 tests, 0 failed, 0 errors.**

### 2026-08-31 — money value on the report too

Client call: the PDF should show what the email shows. `loyalty_report_points_statement`
now calls the same `loyalty.card._get_points_money_value()` and prints it in
parentheses after `points_display` — "120 Points (1,20 €)". Same gate as the
email: an empty string (no per-point reward, or rate 1) renders no parenthesis.
No new translatable term — the amount is formatted by `formatLang`, which follows
the reader's language; the report renders in the **card partner's** language.

`test_the_balance_shows_its_money_value` extended to the report. It asserts on the
digits (`"1.20"` after normalising the decimal comma), not on the helper's own
output: mail and report render in different languages in the test env, so the
helper's string was `$ 1,20` while the report printed `$ 1.20`.

View-only change — `-u` deploys it, no service restart needed.

Suite: **43 tests, 0 failed, 0 errors.**

### 2026-08-31 — coupon report redesigned, stock layout dropped

Client call, from a supplied mockup: the coupon PDF becomes a designed voucher and
loses Odoo's report header and footer.

`loyalty_report_points_statement` no longer *adds a block* to the stock report — it
**replaces the whole body** of `loyalty.loyalty_report`
(`xpath //t[@t-call='web.internal_layout'] position="replace"`). Sections, top to
bottom: company logo + "Loyalty reward" chip, cream congratulations card, navy card
with the code and the barcode, the points card (balance + statement table +
"Current balance" row), a light-blue Validity / Need help band, the thank-you block,
and a navy contact bar (website, phone, city, each `t-if`). Everything is gated the
same way as before: the points card only renders when `_shows_points_statement()`.

New `data/report_paperformat.xml` — `paperformat_loyalty_card`, A4 with 6 mm margins
and no header spacing, assigned to `loyalty.report_loyalty_card`. Without it the
default `base.paperformat_euro` keeps a 40 mm top margin reserved for the header we
just removed.

New `loyalty_report_icon` template: nine inline SVG icons (bag, gift, star, receipt,
trophy, calendar, globe, phone, pin, question), called with `icon` and `size`.

Four traps, all hit:

- **Removing `web.internal_layout` removed the `div.article` wrapper**, and that
  wrapper is what `_render_qweb_pdf` looks for. Without it,
  `ir_actions_report._prepare_html` (`ir_actions_report.py:383`) falls through to its
  "no bodies" branch and hands wkhtmltopdf a bare fragment — **no `web.minimal_layout`,
  so no `<meta charset>` and no report stylesheet**. Symptoms: every accent rendered
  as mojibake (`rÃ©duction`) and the whole page fell back to a serif font. Fix: keep
  the `class="article"` div with its `data-oe-model` / `data-oe-id` / `data-oe-lang`
  attributes, copied from `web.basic_layout`. **Do not drop it.**
- **Font Awesome does not render in the PDF** — hence inline SVG. The `fa` classes
  come from `web.report_assets_common`, which wkhtmltopdf fetches over HTTP; the
  glyphs did not come out. SVG needs nothing fetched.
- **`/logo?company=%d` renders as an empty box.** Use `image_data_uri(company.logo)`,
  which is what `web.external_layout` itself does (`report_templates.xml:309`).
- **wkhtmltopdf 0.12.6 is QtWebKit — no flexbox.** The whole layout is `<table>` plus
  inline styles on purpose; Bootstrap's `row`/`col` would collapse.

French: 18 new terms in `i18n/fr.po`. Two already-translated terms (`Program`, `Used`)
printed in English until the **existing entries got a second `#:` reference line**
pointing at `loyalty_report_points_statement` — model-term translations are matched
per record, so an entry that only names the portal view never reaches the report view. (`Program`'s second reference was dropped again on 2026-09-01 when the
column was removed from the report; `Used` keeps its.)
Deploy with `-u aa_loyalty_points --i18n-overwrite`.

`test_report_drops_the_stock_header_and_footer` pins the layout change. Suite:
**44 tests, 0 failed, 0 errors.**

Views and data only — no service restart needed.

### 2026-09-01 — Program column dropped, email out of the thank-you block

Two client calls, both trims on the printed card.

**1. The statement table loses its Program column.** Header cell and body cell both
removed, so the table is now Date / Description / Earned / Used. The column carried
`o.program_id.name`, which is the *card's own* program repeated on every row — the card
belongs to exactly one program, so every row printed the same value.

The `"Program"` entry stays in `i18n/fr.po`, because `portal_my_loyalty` still uses it;
only its second `#:` reference line — the one naming
`loyalty_report_points_statement` — was removed. That second line was added on purpose
when the report was designed (model-term translations are matched per record, so an
entry naming only the portal view never reaches the report view); with the column gone
it has nothing left to match.

**2. The thank-you block loses the contact email.** The `contact` widget now asks for
`["address"]` instead of `["address", "email"]`. The address stays.

Between this and the "Need help?" trim earlier today, `company.email` no longer appears
on the card at all. `company.phone` still does, once, in the contact bar.

Views and translations only — deploy with `-u aa_loyalty_points --i18n-overwrite`,
no restart.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-09-01 — barcode no longer overflows its card

The white barcode box ran flush to the right edge of the navy card, eating the card's
36 px padding on that side.

**Cause.** `t-options="{'widget': 'barcode', 'width': 600}"` does not set a *display*
width — 600 is the pixel width of the PNG the converter generates
(`ir_qweb_fields.py:797`). The `<img>` it emits carries no width attribute and no CSS,
so it prints at its natural 600 px, which is wider than the 54 % table cell it sits in,
and overflows.

**Fix.** The converter copies through any option named `img_<attr>` whose suffix is in
`safe_attrs` (`ir_qweb_fields.py:803-805`), and `style` qualifies:

```
t-options="{'widget': 'barcode', 'width': 600, 'height': 110,
            'img_style': 'width: 100%; height: auto; display: block;'}"
```

The image now scales to whatever the cell gives it, keeping its aspect ratio, and the
box sits inside the card padding. `width: 600` stays — it is the source resolution, and
lowering it coarsens the bars rather than shrinking the print.

**Reverted the same day:** an earlier read of "reduce the size of the barcode section"
as a styling request had cut the card padding to `20px 30px`, the code to 23 px, the
headings to 16/15 px and the barcode to `height: 80`. The ask was about the overflow,
so all six of those went back to their original values.

Views only — `-u` deploys it, no restart.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-09-01 — help block trimmed

**"Need help?" keeps only the sentence.** The `company.phone` and `company.email` lines
under it are gone — the phone already prints in the contact bar and the email in the
thank-you block, so the panel was repeating both. What is left is the one line with the
opening hours.

A barcode-card size reduction made in the same pass was reverted; see the entry above.

Views only — `-u` deploys it, no restart.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-09-01 — footer fills its band, help line gets opening hours

Two follow-ups on the printed card.

**1. The white strip under the bar is gone.** The bar reached the bottom of the page
but not the bottom of its *band*: `margin_bottom = 14` reserves 14 mm, wkhtmltopdf
renders the footer document top-aligned inside it, and the bar was only about 10 mm
tall — so roughly 5 mm of page showed beneath it.

Rather than tune `margin_bottom` to whatever height the bar happens to be (it changes
with the font and with how many of website / phone / city are set), the footer now
**fills whatever band it is given**: `height: 100%` on the wrapper chain and on the bar
table, and the navy `#12305a` moved onto `html, body` as well. Any slack in the band is
navy, and the row centres itself vertically. Changing `margin_bottom` now only changes
how thick the bar is.

Verified in the PDF content stream rather than by eye: the bar is the filled rectangle
`(x=0, y=0, w=991, h=48)` on a `991 × 1403` page box — `x=0` to full width, and `y=0`
**is** the bottom edge in PDF coordinates. No gap left to measure.

**2. "Besoin d'aide ?" now carries the opening hours.**

| | |
|---|---|
| en_US | "Our team is at your service, Monday to Saturday, 8am to 5pm." |
| fr_FR | "Notre équipe est à votre écoute du lundi au samedi de 8h à 17h." |

Source string in `report/points_statement_templates.xml`, French in `i18n/fr.po` —
the usual split. Deployed with `-u aa_loyalty_points --i18n-overwrite`; without the
flag the old French stays, since `fr_FR` already had a value.

Views, data and translations only — **no service restart needed** for these two.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-09-01 — contact bar pinned to the page bottom, as a real page footer

Client call: the bar must sit on the very bottom edge, full width. It now does. It
is a **real wkhtmltopdf page footer** (`div.footer`), not a block at the end of the
body — the route the previous entry had written off as impossible on this machine.

**Two dead ends first, both re-confirmed and both about the same thing.** wkhtmltopdf
lays the body out in a viewport of page *width* but unbounded *height*, then slices
it into pages, so there is no such thing as "the bottom of the page" in body CSS:

- `height: 285mm` / `height: 1090px` on a full-page wrapper **table**, with the bar in a
  `vertical-align: bottom` second row. Ignored — identical output to no height at all.
  The legacy `height="1090"` attribute is ignored too. (`min-height` on a div was
  already known not to work; a table is no better.)
- `<html style="height: 0">` in `web.minimal_layout` also kills any percentage height,
  so the classic `height: 100%` sticky-footer cannot even be attempted.

**What actually blocked the real footer was not what the previous entry said.**
That entry blamed `subst()` splitting the temp path on `.` — real, but only half of
it. Two separate faults, both had to go:

**1. `subst()` wipes the footer.** `web.minimal_layout` picks a company's header/footer
with `vars['webpage'].split('.', 4)[3]`. `webpage` is the body temp file's **full
path**, and this user's temp dir is `C:\Users\ANIS~1.ALI\AppData\Local\Temp` — the `~1.ALI`
short name contains a dot, so the split yields `'tmp'`, `children['tmp']` is
`undefined`, and the script clears the node before the `appendChild` throws. The
footer is emptied.

Fixed **from inside our own footer**, one line, no core view touched:

```html
<script type="text/javascript">window.subst = function () {};</script>
```

The head declares `function subst()`; our assignment overwrites the binding before
`onload` fires, so nothing manipulates the node and the footer survives intact. We
need no substitution — no page numbers, one company.

A 35-line inherit of `web.minimal_layout` rewriting the index parse
(`report\.body\.tmp\.(\d+)\.`) was written first and **then deleted**: it fixes the bug
for every report on the machine, but it is a global behaviour change we do not need,
and the stub above already covers our own report. Bring it back only if core report
headers/footers are wanted working generally.

**2. `--header-html` suppresses the footer entirely.** This is the one the previous
entry never found. Odoo passes `--header-html` whenever `_prepare_html` returns a
truthy header — and it *always* does, because the header document is a fully rendered
`minimal_layout` page even when it holds an empty
`<div id="minimal_layout_report_headers"></div>`. With that empty header document
passed, wkhtmltopdf 0.12.6 drops the footer: same body, same footer, the only
difference being `--header-html`, gives 78823 bytes (no bar) against 80281 (bar).
Giving the header real content does not help; only *not passing the flag* does.

New `models/ir_actions_report.py` blanks the header when the headers div is empty, so
Odoo skips the flag:

```python
EMPTY_HEADERS = re.compile(r'<div id="minimal_layout_report_headers">\s*</div>')
```

Scoped by construction: it can only fire on a report that has no `div.header` at all,
where the header document was empty anyway.

**Layout.** `paperformat_loyalty_card` reserves `margin_bottom = 14` (was 0) for the
band. The footer draws flush to the bottom edge on its own — wkhtmltopdf puts footer
content at the bottom of the margin area — so nothing pins it. Full bleed comes from
the footer document being its own page at full width; the old `margin: 0 -100px`
hack is gone, replaced inside the footer by

```css
html, body, #minimal_layout_report_footers, #minimal_layout_report_footers > div {
    margin: 0 !important; padding: 0 !important; max-width: none !important; width: 100% !important;
}
```

which cancels the Bootstrap `.container` padding that `minimal_layout` puts on `body`.
(That block gained `height: 100%` and the navy background the same day — see the entry
above.)

**`models/` changed, so the live service needs a restart** — see "Deploying a change
to `mconfort`". `Restart-Service` failed here for lack of elevation; the running
service still has the old `ir.actions.report` and will keep printing without the bar
until someone restarts it from an elevated shell.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-09-01 — the remaining borders: a Bootstrap 5 reboot rule wkhtmltopdf paints

Client report, after the previous entry: sections *still* have borders. They were
never ours. The report CSS bundle (`web.report_assets_common`) carries Bootstrap
5's reboot rule verbatim:

```css
thead, tbody, tfoot, tr, td, th { border-color: inherit; border-style: solid; border-width: 0; }
```

`border-style: solid` at **width 0**. wkhtmltopdf 0.12.6 (QtWebKit) paints that
border anyway, emitting a zero-thickness filled rectangle per cell edge; PDF
viewers draw a zero-height fill as a hairline. Result: a thin outline around
**every `<td>`**, coloured `inherit` — so our navy `#12305a` from the article div.
Since the redesign is built entirely out of nested tables, that is one box per
layout cell, which is exactly what looked like "borders on the sections".

Odoo knows about the bug — `web/.../reports/bootstrap_review_report.scss:106`
comments "still has its borders visible in PDFs" — but only patched
`.table-borderless`, never bare tables.

Fix is one rule, inside the report body:

```html
<style>table, thead, tbody, tfoot, tr, td, th { border-style: none; }</style>
```

**No `!important`, deliberately.** Same specificity as Bootstrap's rule but later
in the document, so it wins; and inline `style="border-left: 1px solid ..."` on
our four divider cells still beats both. `!important` would have killed the
dividers.

Measured on the rendered PDF by counting thin filled rectangles in the content
stream: **145 before, 7 after** — the 7 being our 4 dividers (each painted twice,
plus the one horizontal rule), now at a real 1 px.

Suite: **44 tests, 0 failed, 0 errors.**

### 2026-08-31 — borders dropped, contact bar bled to the page edges

Client report: "the sections got borders" and "the footer isn't full bottom".

**Borders.** Removed the 1 px outline around the points card (now a plain
`#fcfdfe` block) and the rule under each statement row. The two internal dividers
stay — they are in the mockup.

**The bar.** Now full-bleed: `paperformat_loyalty_card` drops its left/right
margins to 0, the content wrapper carries the side padding instead, and the bar
sits in a `margin: 0 -100px` block so it runs past `minimal_layout`'s Bootstrap
`.container` and is clipped at the page edge.

**Why it is not pinned to the bottom edge.** Three attempts, all rejected:

- **The real page footer (`div.footer`) does not work on this machine.**
  `web.minimal_layout` picks which footer to show with
  `footer.children[vars['webpage'].split('.', 4)[3]]`. `webpage` is the body temp
  file path, and this user's temp directory is `C:\Users\ANIS~1.ALI\AppData\Local\Temp` — **the `~1.ALI` short name
  contains a dot**, so the split lands on the wrong field, the lookup returns
  `undefined`, and the script empties the footer it just cleared. Verified by
  rendering the same footer file with `onload="subst()"` stripped: the bar prints.
  Core's own reports hit this too — that is why the invoice's contact bar lives in
  its *body*, not its footer. Do not reach for `div.footer` here.
- **`position: fixed` is dropped** by wkhtmltopdf 0.12.6 — nothing rendered at all.
- **`min-height` on the content wrapper is ignored** — 258 mm, 266 mm, 1045 px and
  1180 px all produced the same page.

Suite: **44 tests, 0 failed, 0 errors.**

---

## Running the tests

```powershell
& "C:\Program Files\Odoo 19.0.20260724\python\python.exe" `
  "C:\Program Files\Odoo 19.0.20260724\server\odoo-bin" `
  -c "C:\Program Files\Odoo 19.0.20260724\server\odoo.conf" `
  -d mconfort -u aa_loyalty_points --test-enable `
  --test-tags=/aa_loyalty_points --stop-after-init --no-http `
  --http-port=8269 --logfile=- --log-level=test
```

The odoo service does not need stopping; registry signalling makes it pick up the
new registry (see the server-root `CLAUDE.md`).

**`--http-port=8269` is required, not optional.** `tests/test_portal_loyalty.py`
is an `HttpCase` and needs a live HTTP daemon. `--test-enable` spawns one even
under `--no-http` (`odoo/service/server.py:653`, "some tests need the http daemon
to be available"), but it binds `config['http_port']`, which is 8069 and already
held by the running `odoo-server-19.0` service. Without an override the run either
fails to bind or the tests talk to the live service instead of the test registry.
Any free port works.

**A `SerializationFailure` on startup is transient.** `psycopg2.errors.SerializationFailure:
n'a pas pu sérialiser un accès à cause d'une mise à jour en parallèle` happens when
the running service is picking up a previous registry change at the same moment.
Just run the command again.

Two ERROR lines appear on every `mconfort` run and are **not ours**:
`Model ai.embedding has no table` (the documented pgvector gap) and
`Some modules are not loaded ... ['website_sale_default_order', 'website_snippets']`
(missing manifests). They predate this module.

### Install state

- `mconfort` — **installed 2026-08-31**, working database, suite runs here.
- `dev` — installed 2026-08-31. **The suite cannot run there:**
  `ProductCommon.setUpClass` archives every pricelist, and `dev` has an active
  program pinned to one (id 2, "Code de remise", `promo_code`), so setUpClass
  raises `UserError: Cette liste de prix ne peut pas être archivée, car elle est
  utilisée dans des programmes promotionnels actifs`. That is `dev`'s own data,
  not our code. `mconfort` has no such program, which is why it works there.
- `dt_loyalty_test` — scratch DB with demo data, created 2026-08-31 while
  `mconfort` was not yet cleared for use. Disposable, drop it when convenient.

### What is live on `mconfort`

| | |
|---|---|
| Programs | 1 `ewallet` ("Gift Cards", **0 cards**), 1 `next_order_coupons` (**1046 cards**), 1 archived `promotion` |
| Cards with a balance | 397 |
| Points already redeemed | 17538 across 18 order lines |
| Confirmed orders / history lines | 494 / 427 |
| Credit notes already posted | 49 — untouched, only new ones trigger recovery |
| Portal users | 49, of which **28** hold a card and now see a balance |

Everything real here runs on `next_order_coupons`. An earlier version of this file
claimed the eWallet was the active program — it is not, it has no cards at all.

### Deploying a change to `mconfort`

**A `-u` run does not reach the live site for Python changes.** `odoo-bin -u`
updates the database from a *second* process; the running `odoo-server-19.0`
service has already imported `models/` and `controllers/` and will not re-read
them. Views, templates and data are stored in the database and go live
immediately, so a change touching both leaves the site inconsistent — a new
template rendered by an old controller raises a 500.

After any change under `models/` or `controllers/`:

```powershell
Restart-Service odoo-server-19.0 -Force   # needs elevation
```

---

## Lot D — points on credit notes (done)

Posting a customer credit note now takes loyalty points back, and returns the
points the order had spent. Neither `loyalty` nor `sale_loyalty` did any of this:
before this module, a refunded customer kept both the money and the points.

### Files

| File | Role |
|---|---|
| `models/account_move.py` | the mechanism: `_post` applies, `button_draft` / `button_cancel` revert |
| `models/sale_order.py` | `_action_cancel` reverts the credit notes first, so points are not subtracted twice |
| `models/loyalty_program.py` | `refund_policy`, `allow_negative_points`, `BALANCE_PROGRAM_TYPES` |
| `models/loyalty_history.py` | portal URL for `order_model == 'account.move'` |
| `views/loyalty_program_views.xml` | "Credit Notes" group on the program form |
| `__init__.py` | `uninstall_hook` purging `order_model == 'account.move'` history |

### How it works

`account.move._post()`, for each `out_refund`:

1. **Skip if already processed.** The `loyalty.history` lines this module writes
   carry `order_model='account.move'` and `order_id=move.id`; their presence *is*
   the processed flag. No extra field, no extra model.
2. **Source orders** = `move.invoice_line_ids.sale_line_ids.order_id`. Empty means
   a manual credit note: log INFO and stop, never recover on a guess.
3. **Ratio per order** = refunded untaxed over order untaxed, both sides excluding
   reward lines, clamped to `[0, 1]`.
4. **Per card** on that order's history: `taken = ratio × issued` rounded DOWN,
   `given = ratio × used` rounded UP. `refund_policy` overrides the ratio: `full`
   forces 1, `none` skips the card.
5. **Cap:** unless `allow_negative_points`, `taken` is capped at the balance and
   the shortfall is posted as a message on the card.
6. `card.points += given - taken`, one history line per card.
7. **Archive a spent-out coupon.** If the program is not nominative, the card was
   generated by this order, it has never been used and its balance is now zero or
   below, the card is archived. `button_draft` un-archives it.

### Decisions taken (PRD §10)

- **#2 rounding** — customer's favour, as proposed.
- **#3 gift cards and eWallets** — *deduct the balance*, capped like everything
  else, never archived: they are the customer's own persistent instrument.
- **`next_order_coupons`** — in scope despite PRD §D8, and additionally archived
  when spent out and unused. See the box at the top of this file.

`loyalty`, `gift_card`, `ewallet` and `next_order_coupons` share one code path via
`BALANCE_PROGRAM_TYPES`; the remaining four program types keep no persistent
balance and are skipped.

### Deliberate deviation from the PRD

§D3 specifies a fallback through `move.reversed_entry_id`. **Not implemented — it
is unreachable.** See "Design notes" below.

### Tests — `tests/test_refund_points.py`, 16 tests

Two classes. `TestRefundPoints` (12) covers PRD §D10 in full plus
`refund_policy='full'`; its fixture is a `loyalty` program giving 10 points per
unit of Product C, so a 10-unit order is 1000 and earns 100 points.
`TestRefundNextOrderCoupon` (4) covers the archiving path: a full refund archives
an unused coupon, a partial one leaves it usable, reset-to-draft brings it back,
and an already-used coupon is deducted but kept.

---

## Lot C — points statement (done)

Both records named by the client are **modified in place**, not recreated.

| External ID | What changed |
|---|---|
| `loyalty.loyalty_report` | QWeb template, inherited — balance and statement table inserted after the barcode |
| `loyalty.mail_template_loyalty_card` | `body_html` replaced with the statement wording |

No new `ir.actions.report`, no new `mail.template`. The email already carries the
coupon PDF through its `report_template_ids`
(`loyalty.report_loyalty_card` → `loyalty.loyalty_report_i18n` →
`loyalty.loyalty_report`), so extending that one template makes the attachment a
statement with no extra wiring, and keeps the per-recipient language the `_i18n`
wrapper already handles.

### Files

| File | Role |
|---|---|
| `models/loyalty_card.py` | `_shows_points_statement()`, `_get_statement_history()` |
| `report/points_statement_templates.xml` | inherits `loyalty.loyalty_report` |
| `data/mail_template_data.xml` | overrides `body_html` on the stock template |

### Behaviour

- The statement block only renders when
  `program_type in ('loyalty', 'gift_card', 'ewallet')`. Coupon and promotion
  printouts and emails are unchanged.
- History is capped by the `aa_loyalty_points.statement_max_lines` config
  parameter, default **50**, `0` meaning no cap. Past the cap the report prints
  "Showing the last N of M movements."
- A card with no movements still renders: balance, then "No movement recorded on
  this card yet."

### Subject left as it is — decided 2026-08-31

`subject` stays Odoo's **"Your reward coupon from {{ object.program_id.company_id.name }}"**.
Client decision: keep it, on the condition that the new body fully replaces the old
one rather than sitting alongside it. Verified — the override is a plain field
write, so both stock passages ("Here is your reward from", "Use this promo code")
are gone from the stored `body_html`. `test_our_body_replaces_the_stock_one` guards
that and will fail if the stock wording comes back.

`email_from` is untouched, still `{{ object.program_id.company_id.email }}`.
PRD §C2 wants `{{ object._get_mail_author().email_formatted }}`, but changing it
would change the sender on coupon emails too. Not done, no decision taken.

### Tests — `tests/test_statement.py`, 8 tests

Covers PRD §C3. Rendering is checked with `_render_qweb_html` rather than
`_render_qweb_pdf` so the suite does not depend on wkhtmltopdf being installed.

### Not built from PRD §C2

The optional phase-2 cron and the `last_statement_date` field on `loyalty.card`.
PRD §10 decision 6 keeps the cron off by default, so nothing was written. The two
live trigger paths are the manual `action_coupon_send` on the card and a
`loyalty.mail` line using the native `points_reach` trigger — both already work
with the modified template.

---

## Lot B — sale order (done)

### Files

| File | Role |
|---|---|
| `models/sale_order.py` | `available_coupon_count`, `action_view_available_coupons()` |
| `views/sale_order_views.xml` | `fa-ticket` stat button, `loyalty_card_view_list_dialog` |

### Behaviour

- **Stat button** next to the native Gift Cards one, hidden at zero, restricted to
  `sales_team.group_sale_salesman`. "Available" means: card belongs to the
  customer or one of its children, `points > 0`, active program, not expired,
  company compatible. No `program_type` filter — any card with a usable balance
  counts.
- **Clicking it opens a dialog**, not a full list view: `target='new'`,
  `dialog_size='large'`, its own read-only list (code with a copy-to-clipboard
  button, balance, expiry, program, customer).
- **No points block under the totals.** Removed 2026-08-31 — `sale_loyalty`
  already prints the same figures there. See the change log.

### Tests — `tests/test_sale_order_points.py`, 6 tests

Cover the count (child-partner rollup, expired/empty card exclusion, no card at
all), the action's domain and `target='new'`, and the N+1 criterion — asserted as
a property rather than an absolute number: the query count to read
`available_coupon_count` over 80 orders must equal the count over 8.

## Lot A — portal (done)

### Files

| File | Role |
|---|---|
| `controllers/portal.py` | `loyalty_count` counter, `/my/loyalty`, widened `cards_per_programs` |
| `views/portal_templates.xml` | home tile, the page, breadcrumbs, `loyalty_buttons_icon` |
| `static/src/img/voucher.svg` | client-supplied icon, used by the tile and the sidebar |

### Routes

- **`/my/home` tile** — "My Points", `placeholder_count='loyalty_count'`, links to
  `/my/loyalty`, icon `voucher.svg`. A customer with no card never sees it:
  `portal.portal_docs_entry` keeps the card `d-none` while the counter is zero.
  It sits **last on the page**, inside `portal_common_category` after Addresses and
  Connection & Security — not among the document tiles in
  `portal_client_category` where every core module puts its own. Inherit priority
  `90` keeps it last within that div.
- **`/my/loyalty`** and **`/my/loyalty/page/<int:page>`** — the only page. One
  subtotal per point unit, a compact card list (program, masked code, balance,
  expiry), then the **history table inline**: Document, Date, Program, Earned,
  Used, paged and sorted with the native `_get_loyalty_searchbar_sortings()`.
  Redirects to `/my` when the customer has no card.

There is deliberately **no second route** for the history — the client asked for
the movements on the page itself rather than behind another click. A `Document`
cell falls back to the raw description when the line has no source document, so
manual balance adjustments still read sensibly.

The per-card page `/my/loyalty_card/<id>/history` is the loyalty module's own and
is reused untouched; the card list does not link to it.

Both read `_get_portal_loyalty_card_domain()`, which is wider than Odoo's — see the
design note on the card domain. The native sidebar is fed from the same domain so
the two never disagree.

### Tests — `tests/test_portal_loyalty.py`, 12 tests

Covers PRD §A4 in full, plus code masking, archived-program exclusion, and the two
added with the widened domain: a `next_order_coupons` card shows on the page and in
the sidebar, and no `/loyalty/static/src/img/` reference survives on `/my`. Built
on `HttpCase` with three portal users: Alice (two programs), Bob (one, used as the
foreign customer), Carol (none).

---

# Design notes

Every non-obvious decision in the source, collected here so the reasoning survives
independently of the code.

## `__init__.py` — the uninstall hook

Purges the history lines this module wrote from customer credit notes. It mirrors
`sale_loyalty`'s own uninstall hook: once this module is gone, nobody resolves
`order_model == 'account.move'` history lines any more, and the portal would show
orphaned references.

It removes the **trace**, not the **effect** — points already taken back are not
restored. That is the same contract `sale_loyalty` has, and it must be said in the
README: uninstalling does not restore balances.

## `models/loyalty_program.py` — `BALANCE_PROGRAM_TYPES`

`('loyalty', 'gift_card', 'ewallet')` are the program types that keep a persistent
balance on the card. The other five (`promotion`, `coupons`, `promo_code`,
`buy_x_get_y`, `next_order_coupons`) hold no balance: nothing to take back on a
credit note, nothing to put on a statement. The constant lives here rather than in
`account_move.py` because both Lot D and Lot C need it.

## `models/account_move.py` — `_apply_loyalty_refund`

The history lines written here double as the "already processed" flag, so posting
the same credit note twice never recovers points twice. No separate boolean field.

**Why there is no `reversed_entry_id` fallback.** PRD §D3 specifies one, but it is
unreachable. Source orders come from the credit note's own invoice lines only,
because `account.move.line._copy_data_extend_business_fields`
(`addons/sale/models/account_move_line.py:37-39`) copies `sale_line_ids` onto the
reversal — so any credit note made by the reversal wizard is already covered by the
primary path. A hand-built credit note has no line link at all, and no per-order
ratio could be derived from it either. Implementing the fallback would find the
orders but always compute a ratio of zero.

## `models/account_move.py` — archiving a spent-out coupon

A `next_order_coupons` card exists only to be spent on a later order. Once a credit
note takes its points back there is nothing left to spend, and leaving a zeroed
card on the customer's account is misleading. So after the points move, the card is
archived when **all four** hold: the program is not nominative, the card was
generated by the order being refunded (`card.order_id == order`), it has never been
used (`use_count == 0`), and its balance is now zero or below.

Each condition earns its place. Nominative excludes loyalty cards and eWallets — a
customer's own persistent card must never vanish. `card.order_id == order` stops a
refund on one order from touching a coupon another order produced. `use_count`
keeps a coupon the customer has already spent, so its history stays coherent. And
the balance test means a partial refund leaves a still-usable coupon alone.

This mirrors `sale_order._action_cancel`, which unlinks the same class of coupon
when the order is cancelled outright.

## `models/account_move.py` — `_revert_loyalty_refund`

Undoes `_apply_loyalty_refund` when a credit note leaves the posted state. It is a
no-op on moves that were never processed, which is what makes it safe to call from
three places: `button_draft`, `button_cancel`, and `sale.order._action_cancel`.

## `models/account_move.py` — `_get_loyalty_refund_ratio`

Both sides of the fraction exclude reward lines. Discounts and free products never
earn points, so counting them would skew the ratio.

## `models/account_move.py` — rounding direction

Rounded in the customer's favour on both sides: `taken` DOWN so we never take back
more than owed, `given` UP so we never give back less than owed. PRD §10
decision 2.

## `models/sale_order.py` — why `_action_cancel` is overridden

`sale_loyalty` only purges history lines whose `order_model` is `'sale.order'`,
then subtracts the order's point changes from the cards. Ours sit on
`'account.move'` and would survive, so the points a credit note already took back
would be subtracted a second time. The override undoes the credit notes first,
leaving `super()` as the only thing moving the balance. Without it, an order
cancelled after a refund corrupts the balance — PRD §14 lists this as the
highest-severity risk.

## `models/loyalty_history.py` — only the URL is overridden

`_get_order_description` needs no override: the base implementation already returns
`account.move.display_name`, which is the credit note's name. Only
`_get_order_portal_url` has to learn about `account.move`.

## `models/loyalty_card.py` — the statement cap

`DEFAULT_STATEMENT_MAX_LINES = 50` is the fallback when
`aa_loyalty_points.statement_max_lines` is not set. An old card can carry hundreds
of movements and the statement prints on the coupon report; without a cap a single
card turns into a several-hundred-page PDF. Set the parameter to `0` to print
everything.

`_get_statement_history` does not sort: `loyalty.history._order` is `'id desc'`, so
`history_ids` is already newest first.

`_shows_points_statement` exists so the gate is expressed once in Python rather
than duplicated as a program-type tuple inside the QWeb template and the mail
template.

## `models/ir_actions_report.py`

One override, `_prepare_html`, and it exists for one reason: **Odoo always passes
`--header-html` to wkhtmltopdf, and on 0.12.6 an empty header document suppresses the
page footer.** `_prepare_html` renders the header through `web.minimal_layout` whether
or not any `div.header` was found, so the result is a full HTML page wrapping an empty
`<div id="minimal_layout_report_headers"></div>` — truthy, so `if header:` passes and
the flag goes on the command line.

Measured on this report: same body, same footer, the only difference being the flag,
78823 bytes without the bar against 80281 with it. A header with real content in it
does not help; only dropping the flag does.

So the override blanks `header` when the headers div is empty. It cannot affect a
report that has a header — the regex only matches the empty div — and for a report
that has none, the document it removes was empty anyway.

This is what makes the contact bar a real page footer rather than a block at the end
of the body, which is why it can sit on the bottom page edge at all.

## `report/points_statement_templates.xml`

**Replaces** the body of the stock coupon report (`loyalty.loyalty_report`, printed by
the `loyalty.report_loyalty_card` action) with the designed voucher, and drops
`web.internal_layout` with it — so the PDF carries no Odoo report header or footer.

The `class="article"` div that wraps the whole body is **not decoration**: it is the
hook `ir_actions_report._prepare_html` looks for. Lose it and the report is handed to
wkhtmltopdf without `web.minimal_layout`, which means no `<meta charset>` (accents turn
to mojibake) and no stylesheet. Its `data-oe-lang` is also what makes the per-recipient
language of `loyalty.loyalty_report_i18n` reach the PDF.

Layout is tables plus inline styles because wkhtmltopdf 0.12.6 is QtWebKit: no flexbox,
so Bootstrap's grid collapses. Icons are inline SVG (`loyalty_report_icon`) because the
Font Awesome glyphs do not come out in the PDF, and the logo is embedded with
`image_data_uri(company.logo)` because `/logo?company=N` renders as an empty box.

`aa_loyalty_points.paperformat_loyalty_card` is assigned to the report action so the
40 mm top margin `base.paperformat_euro` reserves for a header does not stay behind
once the header is gone. Its `margin_bottom = 14` reserves the band the contact
bar prints in: the bar is a real `div.footer`, so wkhtmltopdf draws it flush to the
bottom page edge without any CSS pinning it.

The one-line `<style>` block at the top of the body is not cosmetic. Bootstrap 5's
reboot sets `border-style: solid; border-width: 0` on every table element, and
wkhtmltopdf paints that zero-width border as a hairline — an outline around every
single `<td>`. Resetting `border-style: none` is what keeps the layout tables
invisible. It carries no `!important` on purpose, so the inline `border-left` /
`border-top` dividers still win.

The old behaviour — a statement block appended to the stock layout — is described
below and no longer holds:

Extends the stock coupon report (`loyalty.loyalty_report`, printed by the
`loyalty.report_loyalty_card` action) with a balance and a statement of the card's
movements. The whole block is skipped for program types that carry no balance, so
coupon and promotion printouts are unchanged.

The report is already attached to `loyalty.mail_template_loyalty_card` through its
`report_template_ids`, so the statement email ships this PDF as is.

## `i18n/fr.po` and `body_html@fr_FR`

Two different mechanisms, because the module owns two different kinds of record.

Our own records — the QWeb views, the `refund_policy` field and its selection, the
`_()` strings — carry `ir_model_data.module = 'aa_loyalty_points'`, so their terms
export and import through `i18n/fr.po` like any module's.

`loyalty.mail_template_loyalty_card` does not. We write a field on a record owned
by `loyalty`, and the exporter groups by `ir_model_data.module`
(`tools/translate.py:1399`), so its terms will always live in the *loyalty* POT.
No `.po` we ship can reach it. The French body therefore travels in the data file
itself, as `body_html@fr_FR` — loaded by `_load_module_terms`
(`ir_module.py:971`), skipped by the record loader (`convert.py:395`).

Consequences worth knowing:

- The `@lang` field must be **plain text (CDATA), with no `type` attribute**.
  `type="html"` fails RelaxNG validation (`import_xml.rng:108`) because that branch
  demands a child element, and `XMLDataFileReader` reads only `field.text` anyway —
  markup as child nodes would be silently truncated.
- Changing the French body later needs `-u aa_loyalty_points --i18n-overwrite`.
  Without the flag the existing `fr_FR` value wins and the edit appears to do
  nothing.
- The same `-u loyalty` hazard noted below applies twice over: it rewrites both
  language slots back to Odoo's. Follow it with `-u aa_loyalty_points --i18n-overwrite`.

The English and French bodies are kept structurally identical — same `t-if`
guards, same `t-out` expressions — so a change to one must be mirrored in the
other. There is no test pinning that; the guard is this note.

## `models/loyalty_card.py` — the money value

`_get_points_money_value()` turns a point balance into an amount, for the line
"Your current balance: 430 Point(s) (4,30 €)".

Odoo has no "what is a point worth" field. The nearest thing that is authoritative
is the program's own reward: a `reward_type == 'discount'` reward in
`discount_mode == 'per_point'` stores, in `discount`, the currency amount granted
per point. That is the rate the customer will actually get at checkout, so it is
the one quoted. Programs whose reward is a percentage, a free product, or a
per-order amount have no per-point rate and the method returns `''`.

`discount in (0, 1)` also returns `''`. A rate of 1 makes the money figure a copy
of the point figure — the eWallet case, where `portal_point_name` is already the
currency symbol.

`formatLang` is used rather than string formatting so the separators follow the
reader's language and the symbol follows `program_id.currency_id`.

Deliberately ignored: `discount_max_amount`, which caps a discount per order. A
capped program would quote more than one order can redeem. No live program sets
it; clamp here if one ever does.

Used in two places, both through this one method: the email body
(`data/mail_template_data.xml`, both languages) and the statement block on the
coupon report (`report/points_statement_templates.xml`). An empty return renders
no parenthesis in either.

## `data/mail_template_data.xml`

**This template is the fallback default for every card send, not just loyalty
cards.** `loyalty.card._get_default_template()` in `sale_loyalty` falls back to it
whenever a program has no communication plan of its own — coupons included. That is
why the balance line is conditional: a coupon send still gets its code and expiry
and nothing nonsensical.

Only `body_html` is overridden. `subject`, `email_from` and `report_template_ids`
are left as the loyalty module defines them.

**The stock data file is not `noupdate`.** `loyalty/data/mail_template_data.xml`
declares the record without `noupdate="1"`, so a bare `-u loyalty` rewrites
`body_html` back to Odoo's. Module load order protects the common cases (`-u all`,
or an install) because `aa_loyalty_points` loads after `loyalty`. If a lone
`-u loyalty` is ever run, follow it with `-u aa_loyalty_points`.

## `controllers/portal.py` — the card domain

`_get_portal_loyalty_card_domain()` is own partner, active program, not expired,
and `program_type` in **`BALANCE_PROGRAM_TYPES`** — deliberately wider than the
`['loyalty', 'ewallet']` the loyalty module hardcodes for its sidebar.

Odoo's narrower list is why `/my/loyalty` was empty for every customer on
`mconfort`: all 1046 cards are `next_order_coupons`, so nothing matched and the
tile never appeared. Program 2 carries `portal_visible = True`, so the shop had
already asked for these to be customer-visible; the core domain simply ignores
that flag and filters on type instead.

`_prepare_home_portal_values` therefore also **recomputes `cards_per_programs`**
with the same domain, overriding what `loyalty`'s own controller put there. Without
that the native sidebar and our page would disagree — the page listing cards the
sidebar beside it hides.

**Icons.** `loyalty.loyalty_buttons` builds its image src as
`/loyalty/static/src/img/{program_type}.svg`, and only `loyalty.svg` and
`ewallet.svg` exist — precisely because the core domain admits only those two
types. Widening the domain 404s that image for every other type.

`loyalty_buttons_icon` rewrites the attribute: the program's own icon for
`loyalty` and `ewallet`, and our `static/src/img/voucher.svg` for everything else.
The home tile uses the same voucher file. Nothing needs declaring in the manifest —
Odoo serves `/<module>/static/...` for any installed module; only asset *bundles*
need registering.

`portal.portal_docs_entry` renders the tile icon inside
`class="o_portal_icon d-none"`, revealed by `portal.portal_docs_entry_layout`
("Use Pictograms") — which in Odoo 19 ships `active="True"`, so the icon **is**
visible out of the box, and is active on `mconfort`. (An earlier version of this
note claimed the opposite.) The sidebar icon is always visible.

**Nothing sizes `.o_portal_icon`** — grepping the whole addons tree for that class
returns no SCSS at all. A tile icon is sized purely by the `width`/`height`
attributes on its own `<svg>` root, which every core icon carries and the
client-supplied file did not. Hence the 64×64 added to `voucher.svg`; its
112.68×122.88 viewBox letterboxes inside that, so nothing is distorted.

The xpath must stay scoped to `//div[hasclass('o_loyalty_card')]//img`. A bare
`//img` silently matches images elsewhere in `portal.side_content` — when a
template that itself inherits is inherited again, the xpath runs against the whole
combined tree — and an earlier version that rewrote such an image to reference
`program` took `/my` down with `KeyError: 'program'`.

## `controllers/portal.py` — totals are never summed across units

Balances are grouped by `point_name` and each unit gets its own subtotal. Loyalty
points and a euro-denominated eWallet do not add up (PRD §10 decision 5). The
grouping dict keeps one sample card per unit so the template can call
`_format_points` on it, which is what renders an eWallet balance as currency
rather than as a bare number.

## `controllers/portal.py` — `sortby` is validated

`sortby` is checked against `_get_loyalty_searchbar_sortings()` and falls back to
`'date'`. The loyalty module's own history route does not do this, so a crafted
`?sortby=anything` raises `KeyError` and returns a 500 there. Ours does not.

## `controllers/portal.py` — why the routes are safe

Every query runs `sudo()` with a hard `partner_id = request.env.user.partner_id.id`
filter, and neither route accepts a partner id or a card id as a parameter. There
is nothing to forge. The consolidated history filters on `card_id in` the
customer's own cards, resolved server-side.

## `views/portal_templates.xml` — two details

The masked code follows the convention in `sale_loyalty.used_gift_card`:
`code[-4:].rjust(14, '⋆')`, which shows the last four characters and masks the
rest. Note this is the opposite of a literal reading of PRD §A2 ("4 derniers
caractères masqués"); the core convention it points at is the one implemented.

The `o_loyalty_total` class on each subtotal card exists as a test seam —
`test_two_programs_give_two_separate_subtotals` counts its occurrences to prove
the units were not merged.

## `models/sale_order.py` — the coupon count is one query

`_compute_available_coupon_count` resolves `child_of` once for the whole recordset
and issues a single `_read_group` on `loyalty.card` grouped by `partner_id` **and**
`company_id`. Grouping by company too is what lets one query serve a mixed-company
recordset without attributing another company's cards to an order. Counts are then
rolled up the partner tree, mirroring `res_partner._compute_count_active_cards`.

`compute_sudo=True` is kept for safety, though it turns out not to be strictly
required: `sale_loyalty/security/ir.model.access.csv` grants
`sales_team.group_sale_salesman` read and write on `loyalty.card`
(`access_coupon_salesman`). PRD §9 assumes the opposite.

## `views/sale_order_views.xml` — `loyalty_card_view_list_dialog`

A standalone list view, deliberately not an inherit of
`loyalty.loyalty_card_view_tree`. Inheriting would push the clipboard button onto
the Coupons menu everywhere, and would carry the core view's
`js_class="loyalty_card_list_view"` and its **Send** button into a dialog that
only exists to read a code off a card.

`target='new'` on a **list** view is supported: `_getActionMode` in
`web/static/src/webclient/actions/action_service.js:644` returns `"new"` for any
view type, so the controller is rendered inside `ActionDialog` unchanged.
`context={'dialog_size': 'large'}` widens it — the key is read at
`action_service.js:1063`.

## `tests/` — choices worth keeping

- Both suites build on `sale_loyalty.tests.common.TestSaleCouponCommon`, the same
  base the core loyalty tests use.
- The Lot D fixture gives 10 points per unit of Product C (100 each, no tax), so a
  10-unit order is 1000 and earns exactly 100 points. Round numbers make the
  proportional assertions readable.
- `test_statement.py` keeps a card on a `coupons` program specifically to assert
  the stock coupon layout is untouched — the regression that matters most, since
  the modified records are shared with the rest of the system.
- Report rendering is asserted through `_render_qweb_html`, not `_render_qweb_pdf`,
  so the suite does not depend on wkhtmltopdf.

## Where portal and backend icons actually come from

Three unrelated systems, worth not confusing:

| What | Location | Source |
|---|---|---|
| `/my` tile icons | `portal/static/src/img/portal-addresses.svg`, `sale/static/src/img/bag.svg`, `account/static/src/img/bill.svg` | Plain per-module SVG files, hand-authored, `width="64" height="64"` **and** a 64×64 viewBox. **Not a library** — each module ships its own. The explicit width/height is not decoration: no CSS sizes `.o_portal_icon`, so an SVG without them falls back to the browser's 300×150. |
| `fa-*` classes (backend stat buttons, e.g. our `fa-ticket`) | `web/static/src/libs/fontawesome` | **Font Awesome 4.7.0** |
| `oi-*` classes | `web/static/lib/odoo_ui_icons` | **Odoo UI Icons v1.2** — Odoo's own font, 69 glyphs, built from Carbon Design System and Material icons via IcoMoon |

Because the tiles are per-module SVGs rather than a font, there is no
`next_order_coupons` glyph to fall back on — which is exactly why widening the
portal domain broke the sidebar image and why `voucher.svg` now lives in this
module.
